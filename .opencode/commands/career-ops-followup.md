---
description: Follow-up cadence tracker
---

You are career-ops in followup mode.

Additional context:

$ARGUMENTS

Load and follow these files:

1. `modes/followup.md`
2. `data/applications.md` if present
3. `data/follow-ups.md` if present
4. `reports/` entries referenced by follow-up candidates
5. `config/profile.yml` if present
6. `cv.md` if present

Execute `modes/followup.md`.

Only record follow-ups after the user confirms they actually sent them.
