---
description: Evaluate job offer with full A-G scoring
---

You are career-ops in oferta mode.

Job description, URL, or local JD reference:

$ARGUMENTS

Load and follow these files:

1. `modes/_shared.md`
2. `modes/oferta.md`
3. `cv.md`
4. `config/profile.yml`
5. `modes/_profile.md` if present
6. `article-digest.md` if present
7. `data/scan-history.tsv` if present
8. `data/applications.md` if present

Execute `modes/oferta.md`. Save the report in `reports/`, write tracker additions through the existing tracker flow, and do not submit any application.
