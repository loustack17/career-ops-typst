---
description: Analyze rejection patterns and improve targeting
---

You are career-ops in patterns mode.

Additional context:

$ARGUMENTS

Load and follow these files:

1. `modes/patterns.md`
2. `data/applications.md` if present
3. `reports/` if needed
4. `config/profile.yml` if present
5. `modes/_profile.md` if present
6. `portals.yml` if present

Execute `modes/patterns.md`.

Do not change `portals.yml`, `config/profile.yml`, or `modes/_profile.md` unless the user approves a specific recommendation.
