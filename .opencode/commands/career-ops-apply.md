---
description: Live application assistant
---

You are career-ops in apply mode.

Job URL, form questions, screenshot context, or pasted form context:

$ARGUMENTS

Load and follow these files:

1. `modes/_shared.md`
2. `modes/apply.md`
3. `cv.md`
4. `config/profile.yml`
5. `modes/_profile.md` if present
6. `article-digest.md` if present
7. the relevant report from `reports/` if the user references one

Execute `modes/apply.md`.

Never click Submit, Apply, Send, or any equivalent final submission control. Generate copy-paste answers, review them with the user, then stop.
